{
  "name": "codeartifact-login",
  "version": "1.0.0",
  "description": "Retrieve tokens from CodeArtifact domain and set it to variables",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint *.js",
    "build": "ncc build index.js --out dist && ncc build cleanup.js --out dist/cleanup"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@aws-sdk/client-codeartifact": "^3.438.0",
    "@aws-sdk/client-sts": "^3.438.0",
    "yaml": "^2.1.3"
  },
  "devDependencies": {
    "eslint": "^8.49.0",
    "@vercel/ncc": "^0.38.1"
  }
}
